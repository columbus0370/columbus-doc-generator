services:
  - type: web
    name: columbus-doc-generator-api
    runtime: python
    buildCommand: apt-get install -y fonts-noto-cjk || true && pip install -r backend/requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: CORS_ORIGINS
        sync: false
    healthCheckPath: /health
